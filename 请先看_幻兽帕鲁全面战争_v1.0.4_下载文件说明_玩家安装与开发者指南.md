# 【请先看】《幻兽帕鲁全面战争》v1.0.4 下载文件说明、玩家安装与开发者指南

更新日期：2026-08-16

对应版本：`v1.0.4`

兼容目标：Windows Steam 版 Palworld Build `24575825`

发布页面：<https://github.com/oguzhan0103/huan-shou-palu-total-war/releases/tag/v1.0.4>

## 先说结论：普通玩家不要把所有文件都下载一遍

如果你只是想把当前公开的机制基座装进游戏测试，只需要下载：

```text
PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip
```

其余 `Core-source`、`OfficialAddons-source`、`AIExperimental-source`、
`manifest.json`、`SHA256SUMS.txt` 都不是普通玩家必须安装的文件。

但是必须提前说明：本次 GitHub Release 是 **source-only 机制基座**，不是包含全部
地图、UI 和商店资产的一键安装成品。公开附件不包含 Pocketpair 游戏资产、Cooked
PAK、UAsset、UE4SS 本体、玩家存档或本机部署文件。

因此：

- 普通玩家可以安装并测试公开的 UE4SS Lua 机制；
- 内容作者和开发者可以使用源码继续制作剧情、任务、代表 NPC、唯一 Boss 和结局；
- 如果没有另外合法构建的三个 Cooked PAK，地图覆盖、势力面板和自定义商店等完整
  画面与数据表效果不会全部出现；
- 不要从不明来源下载别人提供的同名 PAK，更不要覆盖游戏原版 PAK。

## 一、我属于哪类下载者

### 1. 普通玩家或机制测试者

下载：

```text
PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip
```

用途：把 `PalFactionTerritory0` 的 UE4SS Lua 运行时安装到游戏中。

不要下载：三个 `*.manifest.json`、`release-manifest.json`、AI 源码和 Core 开发源码。

### 2. 想体验多帕鲁扩展的玩家

除运行时包以外，再下载：

```text
PalworldTotalWar-v1.0.4-OfficialAddons-source.zip
```

其中目前只有独立的 `PalMultiOtomo0` UE4SS 扩展。它允许使用 `F6` 逐只放出最多
四只辅助帕鲁，使用 `F7` 收回辅助帕鲁。这个扩展不是全面战争 Core 的必需组件。

### 3. 写剧情、任务、势力或唯一帕鲁内容的作者

优先下载：

```text
PalworldTotalWar-v1.0.4-Core-source.zip
```

如果只需要最小作者工具，也可以直接使用运行时包中的：

```text
AuthorSDK/
```

### 4. 开发本地大模型 NPC 对话的人

下载：

```text
PalworldTotalWar-v1.0.4-AIExperimental-source.zip
```

它是 Rust 源码，不是双击即用的 EXE。使用者需要自行安装 Rust 和 Ollama、编译
sidecar，并明确配置其路径。

### 5. 只想核验文件是否完整的人

下载：

```text
SHA256SUMS.txt
```

其他 `*.manifest.json` 和 `release-manifest.json` 主要给自动化工具、维护者和安全审计
使用，普通玩家不需要安装。

## 二、Release 页面每一份文件分别是什么

### 1. `PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip`

这是普通玩家和实机测试者最需要的文件。

压缩包根目录包括：

```text
Mods/        UE4SS Mod 本体
Companion/   游戏外势力状态与 AI 输入操作台
AuthorSDK/   内容作者示例、合同和校验工具
INSTALL.md   简版安装说明
package-manifest.json
```

其中真正由游戏加载的是：

```text
Mods/PalFactionTerritory0/
```

这个包不包含三个 Cooked PAK，所以它不是完整美术、地图和数据表成品。

### 2. `PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip.sha256.json`

这是上一份运行时 ZIP 的机器可读校验记录，包含文件名、字节数和 SHA-256。

普通玩家不需要把它放进游戏。它只用于核验下载文件有没有损坏或被替换。

### 3. `PalworldTotalWar-v1.0.4-Core-source.zip`

这是全面战争机制基座的完整公开源码包，面向开发者和内容作者，包括：

- 势力关系、加入、多势力成员与等级；
- 商人商会、商业额度、采购和好感结算；
- 帕鲁袭击、人类防守、后台袭击记录；
- 帕鲁部落信物与论道；
- 任务、战略世界、结局、NPC 态度和领主护卫接口；
- 唯一性帕鲁开放、归属、毁灭战争、保卫和赎回状态机；
- Companion、合同、作者 SDK、测试和构建脚本。

它不是玩家安装包。不要把整个 `Core-source.zip` 解压进游戏目录。

### 4. `PalworldTotalWar-v1.0.4-Core-source.manifest.json`

这是 Core 源码包的文件清单和逐文件哈希，用于证明打包内容、文件数量和可复现性。

它不由游戏读取，不需要安装。

### 5. `PalworldTotalWar-v1.0.4-OfficialAddons-source.zip`

这是官方附加原型源码包，目前包含：

```text
PalMultiOtomo/mod0/ue4ss/PalMultiOtomo0/
```

`PalMultiOtomo0` 与全面战争 Core 相互独立。当前目标 Build 的自动验证已通过，但本轮
没有重新完成全部实机确认，所以应把它视为可选扩展。

### 6. `PalworldTotalWar-v1.0.4-OfficialAddons-source.manifest.json`

这是附加原型源码包的文件和哈希清单。它不需要放入游戏。

### 7. `PalworldTotalWar-v1.0.4-AIExperimental-source.zip`

这是本地 AI NPC 对话 sidecar 的 Rust 源码包，包括：

- Ollama 和 OpenAI-compatible provider；
- UE4SS 文件桥协议；
- 严格请求/回复 JSON Schema；
- 本地角色包示例；
- 隐私、安全和第三方依赖说明；
- Rust 测试与构建脚本。

它不会把模型变成游戏状态的权威。模型只能生成对话和建议白名单选项；好感、任务、
物品、势力归属与结局仍由确定性 Core 和玩家确认控制。

### 8. `PalworldTotalWar-v1.0.4-AIExperimental-source.manifest.json`

这是 AI 源码包的文件和哈希清单。它不需要放入游戏。

### 9. `release-manifest.json`

这是整个 v1.0.4 Release 的机器可读总表，说明三个源码包的版本、状态、文件数量、
目标 Build、验证范围和明确排除的资产。

它主要给维护者和自动化发布工具使用。

### 10. `SHA256SUMS.txt`

这是 Release 文件的 SHA-256 汇总。下载者可以用它核对文件完整性。

### 11. GitHub 自动生成的 `Source code (zip)` 和 `Source code (tar.gz)`

这两份文件由 GitHub 根据仓库 Tag 自动生成，不是我们整理的玩家安装包，也没有
UE4SS 安装目录说明。

普通玩家请忽略它们。开发者需要 Git 历史时应直接克隆仓库，需要冻结源码快照时才
使用 GitHub 自动生成的 Source code。

## 三、普通玩家安装步骤

### 步骤 1：确认版本并备份

1. 使用 Windows Steam 版 Palworld。
2. 本版兼容目标是 Build `24575825`。
3. 先备份自己的世界存档和现有 Mod。
4. 第一次测试建议使用测试世界，不要直接使用唯一主存档。
5. 安装和更新过程中完全关闭 Palworld、Shipping 进程和 Unreal Editor。

### 步骤 2：安装 UE4SS

安装与当前 Palworld Build 匹配的 Palworld 专用 UE4SS。本项目不在 GitHub Release
中重新分发 UE4SS。

安装成功后，至少应该存在：

```text
Palworld/Pal/Binaries/Win64/dwmapi.dll
Palworld/Pal/Binaries/Win64/ue4ss/
Palworld/Pal/Binaries/Win64/ue4ss/Mods/
```

如果连这些目录都没有，不要继续复制 Mod；先解决 UE4SS 安装问题。

### 步骤 3：下载并校验运行时包

下载：

```text
PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip
```

它的 SHA-256 应为：

```text
0867d0a4ccd2255379b8152ead5e3585aa48cb681daba05b7970032e5a415ebe
```

PowerShell 校验命令：

```powershell
(Get-FileHash -Algorithm SHA256 -LiteralPath '.\PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip').Hash.ToLowerInvariant()
```

输出必须与上面的值完全一致。

### 步骤 4：解压到正确位置

打开运行时 ZIP，把压缩包根目录下的全部内容复制到：

```text
Palworld/Pal/Binaries/Win64/ue4ss/
```

注意是把压缩包里的 `Mods`、`Companion` 和 `AuthorSDK` 放进 `ue4ss`，不要额外套一层
`PalFactionTerritory0-v1.0.4-build24575825-runtime-source` 文件夹。

正确结果至少包括：

```text
Palworld/Pal/Binaries/Win64/ue4ss/Mods/PalFactionTerritory0/enabled.txt
Palworld/Pal/Binaries/Win64/ue4ss/Mods/PalFactionTerritory0/Scripts/main.lua
Palworld/Pal/Binaries/Win64/ue4ss/Mods/PalFactionTerritory0/Scripts/pwft/runtime.lua
Palworld/Pal/Binaries/Win64/ue4ss/Companion/start-companion.cmd
Palworld/Pal/Binaries/Win64/ue4ss/AuthorSDK/validate-content-pack.ps1
```

错误示例：

```text
ue4ss/PalFactionTerritory0-v1.0.4-build24575825-runtime-source/Mods/...
```

如果形成了这种多余嵌套，UE4SS 不会找到 Mod。

### 步骤 5：不要启用 QA Harness

正式运行只需要：

```text
Mods/PalFactionTerritory0/
```

不要复制或启用 `PalFactionTerritoryQAHarness0`。它是开发测试工具，不是玩家组件。

### 步骤 6：从 Steam 启动并检查日志

通过 Steam 正常启动游戏，不要直接运行 Shipping EXE。

首次加载后检查：

```text
Palworld/Pal/Binaries/Win64/ue4ss/UE4SS.log
```

至少应看到类似：

```text
[PalFactionTerritory0] READY
FACTION_PROGRESSION_READY
CONTENT_RUNTIME_READY
```

如果没有这些记录，优先检查：

1. ZIP 是否解压到了 `ue4ss` 根目录；
2. 是否出现了多套一层文件夹；
3. `enabled.txt` 是否存在；
4. UE4SS 是否与当前 Palworld Build 匹配；
5. 是否残留旧版同名 Mod 或重复脚本。

## 四、为什么安装后可能看不到完整地图、UI 或商店

当前公开 Release 没有分发以下三个本机验收所用的 Cooked PAK：

```text
Pal/Content/Paks/LogicMods/PalFactionTerritory0.pak
Pal/Content/Paks/~mods/PalFactionTerritory_FactionEconomyShops_P.pak
Pal/Content/Paks/~mods/PalFactionTerritory_RayneMerchant_P.pak
```

这些 PAK 涉及游戏 Cooked 数据表或资产，不能把本机生成物直接当作纯源码开源附件。

因此，只安装公开运行时包时：

- Lua 机制、状态机、合同、部分原生 Hook 和操作台仍可加载；
- 依赖 Cooked 地图、Widget、商店行或资产绑定的画面可能缺失；
- “没有看到完整 UI”不一定是解压失败，也可能是公开包本来就没有对应 PAK。

内容作者必须使用合法取得的 Palworld Modding Kit／UE 工程和自己有权分发的资产构建
对应 PAK，再分别放入：

```text
Palworld/Pal/Content/Paks/LogicMods/
Palworld/Pal/Content/Paks/~mods/
```

不要覆盖 `Pal-Windows.pak`，不要删除原版 PAK，也不要从不明来源获取同名文件。

## 五、安装可选的多帕鲁扩展

1. 下载 `PalworldTotalWar-v1.0.4-OfficialAddons-source.zip`。
2. 解压后找到：

```text
PalMultiOtomo/mod0/ue4ss/PalMultiOtomo0/
```

3. 将整个 `PalMultiOtomo0` 文件夹复制为：

```text
Palworld/Pal/Binaries/Win64/ue4ss/Mods/PalMultiOtomo0/
```

4. 确认存在：

```text
PalMultiOtomo0/enabled.txt
PalMultiOtomo0/Scripts/main.lua
```

5. 游戏内操作：

- 先按原版方式放出主帕鲁；
- 按 `F6` 逐只增加辅助帕鲁，最多四只；
- 按 `F7` 逐只收回辅助帕鲁，原版主帕鲁保留。

出现问题时在 `UE4SS.log` 搜索：

```text
[PalMultiOtomo0]
```

## 六、使用游戏外 Companion 操作台

运行时包中的：

```text
Companion/start-companion.cmd
```

用于显示当前激活世界的势力、好感度、任务、交易和 Agent 状态，并提供本地 AI 文本
输入入口。它不是必须一直打开的游戏 Mod 进程。

推荐顺序：

1. 先启动游戏并进入世界，让 Core 建立 `State`；
2. 再双击 `ue4ss/Companion/start-companion.cmd`；
3. 浏览器通常会打开本机回环地址；
4. 如果找不到 State，使用 PowerShell 显式指定：

```powershell
powershell -ExecutionPolicy Bypass -File '.\Companion\pwft-companion.ps1' `
  -StateDir '.\Mods\PalFactionTerritory0\State'
```

Companion 只监听本机回环地址。不要把端口暴露到公网，也不要把自己的 `State`、存档
或日志上传给陌生人。

## 七、启用本地 Ollama NPC 对话

普通玩家不使用 AI 对话时可以完全跳过这一节，离线作者对话树仍可工作。

### 准备工具

1. 安装 Rust 工具链；
2. 安装 Ollama；
3. 准备本地模型，例如项目当前默认的 `gemma4:e4b`；
4. 下载 `PalworldTotalWar-v1.0.4-AIExperimental-source.zip` 并解压。

### 编译

在 AI 源码目录执行：

```powershell
cargo fmt --all -- --check
cargo test --locked --all-targets
cargo clippy --locked --all-targets -- -D warnings
cargo build --release --locked
```

生成文件通常位于：

```text
target/release/pal-agent-dialogue.exe
```

### 配置并启动

在同一个 PowerShell 窗口设置：

```powershell
$AiSourceRoot = (Resolve-Path '.\PalworldTotalWar-v1.0.4-AIExperimental-source').Path
$env:PAL_AGENT_EXECUTABLE = Join-Path $AiSourceRoot 'target\release\pal-agent-dialogue.exe'
$env:PAL_AGENT_CHARACTER_PACK = Join-Path $AiSourceRoot 'character-packs\pwft-author-sdk-minimal.json'
$env:PAL_AGENT_MODEL = 'gemma4:e4b'
```

然后进入游戏 `ue4ss` 目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File '.\Companion\pwft-companion.ps1' `
  -StateDir '.\Mods\PalFactionTerritory0\State'
```

安全边界：

- Ollama 默认只允许本机回环地址；
- 模型回复必须通过严格 JSON 白名单；
- 模型不能直接增加好感、完成任务、发物品或提交结局；
- 模型建议的确定性选项仍需要玩家按 `F3` 确认；
- 不要把 API Key 写进仓库、角色包、日志或说明文件。

## 八、开发者从哪里开始

### 推荐方式：克隆仓库

需要提交代码或保留 Git 历史时，不要使用玩家运行时包，直接执行：

```powershell
git clone https://github.com/oguzhan0103/huan-shou-palu-total-war.git
Set-Location .\huan-shou-palu-total-war
```

如果只需要冻结的 v1.0.4 源码快照，可以下载
`PalworldTotalWar-v1.0.4-Core-source.zip`。

### 目录职责

```text
PalFactionTerritory/
  contracts/                 稳定 JSON 合同
  examples/minimal-content-pack/
                             最小无剧情内容包
  mod0/ue4ss/PalFactionTerritory0/
                             游戏实际加载的 Lua Core
  mod0/tests/                Lua 自动测试
  companion/                 游戏外只读状态与 AI 输入操作台
  scripts/                   验证、打包和部署辅助脚本
  tools/                     内容包与源码校验工具

PalMultiOtomo/               可选多帕鲁扩展
PalAgentDialogue/            本地 AI sidecar
tools/build-source-release.py
                             公开 source-only 打包器
```

### 安装依赖并运行全量验证

在仓库根目录执行：

```powershell
npm ci
npm test
```

当前 v1.0.4 冻结基线应通过：

```text
68 个 Lua 源文件
61 个 Lua 测试
340 个受跟踪文本文件扫描
```

Faction Core 专项验证：

```powershell
powershell -ExecutionPolicy Bypass -File '.\PalFactionTerritory\scripts\verify-mod0.ps1'
```

多帕鲁扩展验证：

```powershell
powershell -ExecutionPolicy Bypass -File '.\PalMultiOtomo\scripts\verify.ps1'
```

AI sidecar 验证：

```powershell
Set-Location .\PalAgentDialogue
cargo fmt --all -- --check
cargo test --locked --all-targets
cargo clippy --locked --all-targets -- -D warnings
cargo build --release --locked
```

### 修改 Lua Core

主要源码位于：

```text
PalFactionTerritory/mod0/ue4ss/PalFactionTerritory0/Scripts/pwft/
```

配置入口：

```text
PalFactionTerritory/mod0/ue4ss/PalFactionTerritory0/Scripts/pwft/config.lua
```

修改原则：

1. 机制、剧情和原生资产绑定分层；
2. 基座不内置具体剧情文本；
3. 不让模型成为任务、好感、物品或结局权威；
4. 每个持久事件使用稳定且唯一的 ID；
5. 失败关闭，不用旧 Actor 或不确定映射继续写状态；
6. 新增机制时同时补测试和合同；
7. 不把 PAK、UAsset、存档、日志、Crash、EXE/DLL 或本机路径提交到公开仓库。

### 构建玩家可复制的运行时源码包

```powershell
powershell -ExecutionPolicy Bypass -File '.\PalFactionTerritory\scripts\build-mod0-package.ps1'
```

输出位于：

```text
PalFactionTerritory/artifacts/releases/
```

构建器会运行 Core 验证并检查作者 SDK，但产物仍是 source-only，不会自动生成三个
Cooked PAK。

### 构建三个公开源码包

先只验证白名单：

```powershell
python .\tools\build-source-release.py --verify-only
```

再构建到指定目录：

```powershell
python .\tools\build-source-release.py --output .\release\v1.0.4-local
```

不要覆盖已经发布的 v1.0.4 附件；代码有变化时应提升版本并重新生成清单和哈希。

## 九、内容作者怎么制作剧情与玩法“密钥”

### 1. 取得模板

可以使用仓库中的：

```text
PalFactionTerritory/examples/minimal-content-pack/
```

也可以使用运行时包中的：

```text
AuthorSDK/minimal-content-pack/
```

### 2. 必须修改的内容

至少应修改：

- `manifest.lua`：包 ID、命名空间、版本、依赖和能力；
- `localization_keys.lua`：全部文本键；
- `localization_catalogs.lua`：中文和其他语言正文；
- `quest_template.lua`：任务阶段、条件、分支和结果；
- `pal_discourse.lua`：帕鲁代表与论道树；
- `strategic_world.lua`：城邦和唯一帕鲁定义；
- `unique_pal_campaign.lua`：唯一 Boss 开放、毁灭目标、NPC 候选势力和赎金；
- `leader_guards.lua`：领主与护卫编组；
- `ending_routes.lua`：结局条件和效果；
- `content_actions.lua`：任务结果允许调用的白名单机制动作。

不要把剧情正文直接写进机制表，正文应放在本地化目录中，通过 key 引用。

### 3. 校验内容包

在仓库中：

```powershell
powershell -ExecutionPolicy Bypass -File '.\PalFactionTerritory\scripts\validate-content-pack.ps1' `
  -PackPath '.\PalFactionTerritory\examples\minimal-content-pack'
```

使用运行时 ZIP 时：

```powershell
powershell -ExecutionPolicy Bypass -File '.\AuthorSDK\validate-content-pack.ps1' `
  -PackPath '.\AuthorSDK\minimal-content-pack'
```

### 4. 安装内容包

把整个内容包文件夹复制到：

```text
ue4ss/Mods/PalFactionTerritory0/Scripts/
```

例如：

```text
ue4ss/Mods/PalFactionTerritory0/Scripts/my-story-pack/content_module.lua
```

在 `Scripts/pwft/config.lua` 中登记模块：

```lua
contentModules = {
    enabled = true,
    modules = {
        "my-story-pack.content_module",
    },
    fallbackLocale = "zh-CN",
}
```

启动后在 `UE4SS.log` 检查：

```text
CONTENT_MODULE ... registered=true activated=true
CONTENT_MODULE_LOADER_READY ... failed=0
```

如果任一领域校验失败，Core 会拒绝整包注册，不会只加载半套内容。

## 十、更新、回滚和卸载

### 更新

1. 完全关闭游戏；
2. 备份：

```text
ue4ss/Mods/PalFactionTerritory0/State/
```

3. 完整替换旧的：

```text
ue4ss/Mods/PalFactionTerritory0/
```

4. 不要把新旧 `Scripts/pwft` 混在一起；
5. 启动后检查版本和 `READY` 日志。

### 卸载

游戏关闭后删除 Mod 自有目录：

```text
ue4ss/Mods/PalFactionTerritory0/
```

如果安装过可选扩展，再删除：

```text
ue4ss/Mods/PalMultiOtomo0/
```

如果内容作者曾给你三个合法构建的 PAK，只删除这三个明确的 Mod PAK，不要删除 UE4SS
中的其他 Mod，也不要删除任何 Palworld 原版 PAK。

Mod 的持久状态位于 Mod 自有 `State` 目录。需要保留机制进度时，卸载前先备份它。

## 十一、常见问题

### 问：Release 文件太多，我到底下载哪个？

普通玩家只下载：

```text
PalFactionTerritory0-v1.0.4-build24575825-runtime-source.zip
```

想用多帕鲁再下载 `OfficialAddons-source.zip`；写代码才下载 `Core-source.zip`；使用本地
AI 才下载 `AIExperimental-source.zip`。

### 问：为什么没有一个完整的一键安装包？

因为本次公开版本坚持 source-only，不直接分发 Pocketpair 游戏资产或包含原始数据表
的 Cooked PAK。当前发布目标是让机制基座和作者接口公开、可审计、可共同开发。

### 问：只装运行时包能不能玩？

可以加载和测试公开 Lua 机制，但依赖三个 Cooked PAK 的地图、UI 和自定义商店表现不
会完整。请把它理解为公开机制基座，而不是全部资产齐全的玩家成品。

### 问：启动后完全没有日志怎么办？

优先检查 UE4SS 是否安装正确、游戏 Build 是否匹配、ZIP 是否多套了一层目录，以及
`enabled.txt` 是否存在。

### 问：有 `READY`，但地图或商店不完整怎么办？

先确认是否缺少三个 Cooked PAK。公开 GitHub Release 本来就不包含它们。

### 问：游戏更新以后还能继续用吗？

不保证。Palworld 更新可能改变 Hook、类名、数据表和 UE4SS 兼容性。游戏 Build 与
`24575825` 不一致时，应先等待兼容性复核，不要拿唯一主存档冒险。

### 问：AI 操作台没有回复怎么办？

检查 Ollama 是否启动、模型是否已经下载、`PAL_AGENT_EXECUTABLE` 和
`PAL_AGENT_CHARACTER_PACK` 是否指向真实文件，以及 Companion 是否找到当前世界的
`State`。AI 不可用时，离线作者对话树仍然保留。

### 问：可以把自己的剧情包发出来吗？

可以。请使用独立命名空间、提升内容版本、通过 AuthorSDK 校验，并确保只分发自己有
权分发的文本、代码和资产。不要打包玩家存档、日志、密钥或 Pocketpair 原始资产。

## 十二、反馈问题时请提供什么

请至少说明：

1. Palworld Build；
2. UE4SS 版本；
3. 下载的附件名称；
4. 实际安装目录结构；
5. 是否安装了三个 Cooked PAK；
6. `UE4SS.log` 中与 `[PalFactionTerritory0]` 相关的错误片段；
7. 问题发生在新世界还是已有世界；
8. 是否同时启用了其他 Mod。

公开日志前请删除 Steam ID、本机用户名、存档名、绝对路径、API Key 和其他私人信息。
