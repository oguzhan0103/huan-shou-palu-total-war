# 唯一帕鲁原生交付事务桥：开发与实机验收

日期：2026-08-22

结论：**P2 的通用交付事务桥和 Build 24575825 当前原生调用路线均已完成。2026-08-22 在受控世界 `oguzhan` 实机验证了：容量预检后只生成一个阿努比斯、异步取得稳定 individual identity、调用原生捕获、再从玩家 Pal 仓库按完全相同的 `PalInstanceID` 回读成功。QA 路线没有直接改 Pal 容器，测试后正式开关恢复关闭，存档从测试前快照恢复为 363/363 文件且 0 哈希差异。后续已新增受信内容包可调用的 P2 生产注册器和付款→交付组合自动验收；正式商品与目标绑定仍由内容密钥提供，不应把机制接口写成完整战役已可玩。**

## 1. 采用的原生路线

Build 24575825 当前 ObjectDump 已确认 `PalPlayerState:GetPalStorage`、`PalIndividualCharacterContainer:FindEmptySlot`、`PalUtility:PalCaptureSuccess` 和 `PalNPCManager:SpawnNPCForServer` 等入口；随后只读仓库探针和本次有写入的交付验收分别补齐了对象链、容量、捕获与精确回读证据。

公开实现 [AdminCommands 的 spawn.lua](https://github.com/dkoz/AdminCommands/blob/main/AdminCommands/Scripts/modules/spawn.lua) 也采用 `SpawnNPCForServer` 后等待生成 Actor，再调用 `PalCaptureSuccess` 的路线。这只能证明路线有先例；其源码明确把 spawn/catch 标为实验性实现，并没有覆盖本项目要求的容量预检、唯一个体回读、失败回滚和重复交易隔离。因此本项目没有直接复制，而是在候选原生调用外增加严格事务边界。

## 2. 本轮实现

- 新增 `_G.PWFT_UNIQUE_PAL_NATIVE_DELIVERY_BRIDGE_V1`，并把同一实例交给受信内容模块。
- P2 `pal-delivery` 载荷新增 Core 战役定义中的确切 `speciesId`；Provider 必须同时匹配 `uniquePalId` 与 species 白名单。
- 当前 Build 绑定必须显式声明并通过：`currentBuildVerified / serverAuthoritativeSpawn / serverAuthoritativeCapture / capacityPreflight / storageVerification / stableIndividualIdentity`。任何一项缺失都拒绝注册。
- 创建前先检查玩家 Pal 容器容量；如果适配器发现同一 delivery 已交付，可按同一 individual key 恢复，而不是重新生成。
- 创建结果必须返回稳定的 `nativeDeliveryId` 和 `individualKey`。Core 在 Provider 接受后保存两者，最终确认时逐项核对。
- 捕获只提交一次。仓库回读暂未看到同一 individual 时，只重试回读与最终回调，不重复创建、不重复捕获，也不要求玩家再次付款。
- 最终回调同时核对 `deliveryId / providerId / authoritySource / bindingId / worldGeneration / nativeDeliveryId / nativeIndividualKey / palDeliveryKey / uniquePalId / speciesId / playerId`。
- 世界卸载时只回滚“已创建但尚未提交捕获”的临时个体；已经提交捕获的记录不做盲目删除。所有运行时对象引用都由适配器持有，不写入 sidecar。
- 明确禁止直接改 Pal 容器、调试捕获 API 和 Palworld 存档写入。

## 3. 自动测试

专项测试覆盖：

- 未核验当前 Build 的适配器失败关闭；
- species 白名单、Provider、目标 binding、route 和 world generation 精确匹配；
- 容量预检后只创建一个个体；
- Core 进入 `awaiting-confirmation` 且 request/individual 都一致后才允许提交；
- 捕获只提交一次，第一次仓库回读未完成后可重试；
- 第二次回读确认同一 individual 后才完成 Core delivery；
- 重放同一交付不再次预检、创建、捕获或回调；
- 错误 individual 和错误 species 回调拒绝；
- 世界卸载只回滚未提交创建；
- 运行时烟测确认实例、全局 API、内容模块上下文、卸载生命周期和 0 原生绑定状态可启动。

专项命令：

```powershell
..\node_modules\.bin\fengari.cmd mod0\tests\unique_pal_world_effect_bus_spec.lua
..\node_modules\.bin\fengari.cmd mod0\tests\unique_pal_native_delivery_bridge_spec.lua
..\node_modules\.bin\fengari.cmd mod0\tests\runtime_smoke.lua
```

完整门禁：

```powershell
python .\tools\verify_mod0.py
powershell -ExecutionPolicy Bypass -File .\scripts\verify-mod0.ps1
git diff --check
```

本轮离线结果：专项测试均 `PASS`；`verify_mod0.py` 退出码为 0；完整 `verify-mod0.ps1` 退出码为 0；公共源码验证汇总为 `77 Lua files / 70 Lua tests / 376 tracked text files scanned`；`git diff --check` 无空白错误。离线结果只证明源码与合同，实机结果单独记录如下。

## 4. Build 24575825 受控实机结果

- 测试世界：`oguzhan`，世界 ID `E0D5ECDC46B379829F8F31A729ACFD92`，运行时代际 4。
- QA 交付：`qa.native-pal-delivery.g4.r1`，物种 `Anubis`，等级 1。
- `SpawnNPCForServer` 第一次返回实体句柄时 identity 尚未就绪；500 ms 后从同一句柄取得 `pal-00000000000000000000000000000001-8E0F25AB4226BF458C679D91DC9EE50C`，未重复生成。
- `PalCaptureSuccess` 第一次提交即接受；随后玩家仓库第一次回读即找到完全相同的 individual key。
- 最终结果：`native-pal-delivery-live-test-verified`，identity/capture/readback 均为 1 次，`directContainerMutation=false`。
- 先前十秒回调被 UE4SS 垃圾回收的问题已通过持有延时与游戏线程回调的强引用修复；本次主世界就绪后未再出现 `Hook threw exception`。
- 测试前快照 363 个存档文件；测试后变更副本已移入可恢复隔离目录，原快照恢复为 363/363、哈希差异 0。
- 安装目录 QA 开关已恢复 `false`，随后重新运行正式部署审计，75 个运行时文件与源码清单一致。

完整摘录与存档恢复边界见 `evidence/live-tests/build24575825-20260822-unique-pal-native-delivery/RESULT.md`。

## 5. 证据分层

| 层级 | 状态 |
| --- | --- |
| 合同与事务语义 | 完成 |
| Lua 源码与运行时接线 | 完成 |
| 自动测试 | 完成 |
| Build 24575825 原生函数签名 | 完成 |
| 当前 Build 原生创建/捕获适配器 | 完成，QA 单路线实机通过 |
| 游戏目录部署 | 完成，正式配置已恢复 |
| 实机 Pal 仓库精确回读 | 完成，Anubis 单路线通过 |
| P2 生产注册接口 | 完成；仅批准五只已确认唯一帕鲁，空涡龙失败关闭 |
| 内容包 Provider/商品/目标绑定 | 基座 0 个；等待内容密钥 |
| 用户接受 | 未执行 |

## 6. 剩余工作

1. 内容作者用生产接口注册正式 P2 world-effect/target binding；空涡龙在 species ID 确认前继续失败关闭。
2. 为商人商会增加正式赎金商品行，并把服务器购买成功回执接到已验收的交付事务桥。
3. 继续补容器已满、生成失败、捕获延迟、仓库回读延迟、重复回调、返回标题和重进世界等失败分支实机。
4. 按内容定义验证等级、满星、满魂、被动词条与战役记录；本轮只证明实体交付与 identity 一致，没有替剧情作者猜写这些数值。
5. 纯开发组合验收已经证明一次服务器购买只产生一次逻辑归属转移和一次实体交付；待正式内容商品生成后补同一路线的实机组合证据。

本轮没有修改原始 PAK，也没有推送 GitHub。实机测试产生的存档变化已保留在可恢复隔离目录；正式存档、正式 QA 配置与安装目录均已恢复并审计。
