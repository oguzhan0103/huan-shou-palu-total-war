# M-I 昼夜势力与剩余中立区记录

状态：**用户规划已记录；M-T 与世界树继续排除在本轮划分之外。**

## M-I 暗属性帕鲁部落

- 区域：M-I (`T_MapMask_k`)。
- 归属：暗属性帕鲁部落，稳定 ID 为 `pwft.faction.dark_nocturnal_pal_tribe`。
- 生态范围：优先复用本体中主要于夜晚刷新的暗属性帕鲁，不为部落制作新帕鲁。
- 夜晚：暗属性帕鲁部落进入实际控制状态；红、绿、蓝关系继续读取玩家与该部落的当前关系。
- 白天：区域切换为安全区，并临时按中立状态处理。
- 白天公共传送：只要本体传送点已经解锁即可使用。
- 夜晚公共传送：继续遵守 MVP 总规则，仅当该部落与玩家当前为敌对关系时禁用。
- 时间来源必须复用本体昼夜循环，不增加第二套 Mod 时间系统。

## 暂定中立区

此前尚未规划的其余地块统一暂定为中立区：

| 区域 | 原生遮罩 | 状态 |
|---|---|---|
| M-B | `T_MapMask_b` | 暂定中立 |
| M-J | `T_MapMask_l` | 暂定中立 |
| M-K | `T_MapMask_m` | 暂定中立 |
| M-M | `T_MapMask_o` | 暂定中立 |

这些地块当前没有所有者势力 ID，关系状态固定为 `Neutral`；以后用户重新划分时可直接替换，不需要迁移或删除势力实体。

## 当前规划边界

- 除 M-T、世界树 T-A / T-B 外，主世界其余区域均已有正式势力归属或暂定中立状态。
- M-T 仍保持“排除／待后续决定”，不自动归为中立。
- 世界树仍保持独立地图与独立区域合同，不混入主世界暂定中立区。

机器可读记录位于 `contracts/territory_assignments.v1.json`。
