# 雷恩商人关系往返与轮询稳定性验收

日期：2026-08-14

游戏版本：Steam Build 24575825

测试世界：已授权的本地测试世界（公开文档匿名化）

## 结论

雷恩帕鲁商人的关系策略已完成源码、自动测试与实机运行时验收：

- `Friendly -> Hostile -> Friendly` 同一轮往返成功；
- 敌对时原生交互被关闭，网络商店不绑定，且不享受商人商会的商业停战；
- 恢复友好后原生交互重新启用，`PFT_Rayne_AllPaldex` 商店行与网络商店绑定恢复；
- 修复后的商人商会状态轮询在关系往返后持续运行 4 分 16 秒，没有再次抛出 Hook 异常；
- 本轮结束后游戏已关闭，正式配置、Palworld 存档和 Mod State 均恢复到测试前快照。

因此，本项的“关系驱动交互/商店开关与往返恢复”判定为 **PASS**。

## 实机证据

### 1. 初始友好

2026-08-14 20:03:11：

- `RELATION_INTERACTION_POLICY relation=Friendly ... interactEnabled=true`
- `NATIVE_MERCHANT_READY ... shopRow=PFT_Rayne_AllPaldex relation=Friendly`
- `ECONOMY_SHOP_BOUND faction=pwft.faction.rayne_syndicate ... catalog=false`

### 2. 切换敌对

2026-08-14 20:03:23 至 20:03:25：

- `RAYNE_RELATION_LIVE_TEST_APPLIED ... previous=Friendly relation=Hostile ... saveWrites=0`
- `RELATION_INTERACTION_POLICY relation=Hostile ... interactEnabled=false`
- `NATIVE_MERCHANT_READY ... relation=Hostile`
- `SHOP_ACCESS_BLOCKED ... interaction=false networkShop=false commercialTruce=false`

这证明敌对雷恩商人不会沿用商人商会柜台的商业停战规则。

### 3. 恢复友好

2026-08-14 20:03:37 至 20:03:39：

- `RAYNE_RELATION_LIVE_TEST_APPLIED ... previous=Hostile relation=Friendly ... saveWrites=0`
- `RELATION_INTERACTION_POLICY relation=Friendly ... interactEnabled=true`
- `NATIVE_MERCHANT_READY ... shopRow=PFT_Rayne_AllPaldex relation=Friendly`
- `ECONOMY_SHOP_BOUND faction=pwft.faction.rayne_syndicate ... catalog=false`

### 4. 轮询稳定性

恢复友好时间为 20:03:39，静置观察截止 20:07:55，共 4 分 16 秒：

- Palworld 进程持续存活；
- `Hook threw exception` 数量为 0；
- `faction_economy_merchant_runtime.lua` 运行时异常数量为 0。

此前状态轮询曾在异常记录值上直接索引，导致重复 Hook 被 UE4SS 移除。本轮将状态统计改为对非表记录 fail-closed：异常值只计入 `invalidRecordCount`，不再让轮询回调抛出。专项测试同时注入函数型哨兵记录，确认 7 个正常柜台统计不受影响。

## 自动验证

最终完整验证：

```text
PASS public source verification: 66 Lua files, 59 Lua tests, 324 tracked text files scanned
PASS Build 24575825 deployment matches manifest, current source, and 64 recorded hashes
```

## 恢复与安全边界

本轮结束状态：

- Palworld 进程：0；
- 正式配置：`rayneMerchant.relationLiveTest.enabled=false`；
- SaveGames：447/447，missing=0，added=0，changed=0；
- Mod State：12/12，missing=0，added=0，changed=0；
- 当前测试后数据未删除，分别保存在本地工作区和 UE4SS 的非公开可恢复隔离目录；
  具体绝对路径不进入公开仓库。

## 尚未冒充完成的部分

- 本轮玩家没有站到独立雷恩商人面前，因此“敌对时实际攻击玩家、按原生 F 无法开店、恢复友好后原生 F 再次打开 288 图鉴目录”的画面级三段演出没有单独重拍；本轮证据覆盖的是原生交互组件、商店绑定和关系往返运行时策略。
- 商人商会 7 个柜台的真实购买、取消、失败购买、非需求品出售、需求品出售、重复 OnRep 去重和商业额度封顶仍属于 B3 的下一轮实机交易验收，不在本项结论中。
