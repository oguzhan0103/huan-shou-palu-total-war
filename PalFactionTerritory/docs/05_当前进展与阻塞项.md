# 当前进展、产物与阻塞项

更新时间：2026-07-21。

## 状态

| 项目 | 状态 | 证据／产物 |
|---|---|---|
| 原目录全量盘点 | 完成 | `evidence/original_workspace_manifest.csv` |
| 既有文本资料复核 | 完成 | 两个原验证脚本均 0 error |
| 游戏安装与 build 复核 | 完成 | Steam build 24181527；本体 PAK 和 Mods 配置存在 |
| 本地 Mod 工具链检查 | 完成 | UE 5.1.1、VS 2022、PMK、UE4SS、PalSchema、FModel、UAssetGUI 已定位；Wwise 实体安装与当前 `.usmap` 缺失 |
| 关键本体资产定向抽取 | 完成 | 173 文件；`evidence/extracted_asset_manifest.csv` |
| 24 个瞭望塔稳定契约 | 完成 | `contracts/tower_territories.v1.json` |
| 本体势力稳定契约 | 完成 | `contracts/factions.v1.json` |
| 关系颜色与动态更新 | 离线完成 | `TerritoryPolicyEngine` + 验证器 |
| 原始／势力地图模式 | 离线完成 | 原始模式不绘制；势力模式策略已验证 |
| 地图势力／塔主／关系信息 | 只读探针已部署 | 本体文本绑定与动态关系标签已验证；地图创建钩子待实机日志确认 |
| 敌对公共传送限制 | 只读探针已部署 | 规则已验证；当前只记录调用，不拦截传送 |
| 入境提示事件 | 判定完成／UI推迟 | 事件无副作用；犯罪 UI 不接入 |
| 可安装游戏 Mod | Mod 0 与原生 LogicMod 骨架已部署 | UE4SS Lua 只读切片 + `pakchunk1001` 空业务骨架；地图写入、传送拦截、存档写入均关闭 |
| 游戏实机验证 | 部分完成 | `READY`、地图 hook、原生 `ModActor_C` 与 22 个瞭望塔 `FastTravelPointID` 已记录；遮罩映射与 2 座未观察塔待补齐 |

## 本轮新增产物

- `README.md`：状态和运行入口。
- `docs/`：盘点、可行性、架构、接入点、风险、当前状态。
- `contracts/`：稳定势力、24 塔区和样例关系事件。
- `src/pal_faction_territory/`：无 UE 依赖的 Python 标准库领域核心。
- `tools/verify_core.py`：零第三方依赖领域规则验证器。
- `tools/verify_native_text_bindings.py`：逐项回查 24 座塔与 7 个势力的本体双语文本。
- `scripts/verify.ps1`：一键运行离线验证。
- `scripts/check-runtime-prereqs.ps1`：只读输出运行时条件。
- `evidence/game_assets_raw/`：定向抽取的本体分析副本，不进入发布包。
- `evidence/*.csv`：原工作区和抽取资产 SHA-256 清单。
- `mod0/ue4ss/PalFactionTerritory0/`：首个只读 UE4SS Lua 运行时切片。
- `scripts/verify-mod0.ps1`：Lua 语法、策略和运行时烟测入口。
- `scripts/build-mod0-package.ps1`：拒绝覆盖既有产物的开发包构建脚本。
- `scripts/install-mod0-dev.ps1`：校验 build、工具哈希和目标冲突后部署的安装脚本。
- `scripts/check-mod0-deployment.ps1`：只读部署结构检查。
- `artifacts/releases/PalFactionTerritory0-mod0-build24181527.zip`：已校验开发测试包。
- `evidence/deployments/mod0-dev-build24181527.json`：31 个新增文件的路径、大小和 SHA-256。

## 最终离线验证结果

2026-07-21 实际复跑：

- 领域核心：`PASS (14 assertions)`。
- 本体文本绑定：`PASS (24 towers, 7 factions)`。
- Python 语法编译：`compileall PASS`。
- 原文本库：91 条主记录、14,716 条原始行、3,744 次双语偏移回读，`errors=0`。
- 原工作区清单：357/357 文件大小与 SHA-256 一致。
- 定向资产清单：173/173 文件大小与 SHA-256 一致。
- 运行时前置检查：游戏与本体 PAK、VS 2022 C++、UE 5.1.1、PMK、Palworld 专用 UE4SS、PalSchema、FModel 和 UAssetGUI 均已定位；当前 `.usmap` 与可用的 Wwise/PMK 集成仍缺失。
- Mod 0 注册表：`PASS (22 regions, 12 factions, 24 towers)`。
- Lua 策略：关系、颜色、迷雾、昼夜和敌对传送决策 PASS。
- Lua 运行时烟测：启动、钩子注册、控制台命令和三个安全门 PASS。
- 包结构：PASS；ZIP SHA-256 `945c8ebac2ce18151601fc0bcbe23661a12c4ac22eede1ee6925a00aeb12ab79`。
- 部署结构：PASS；UE4SS 使用审核过的 Palworld 专用包，归档 SHA-256 `5e0c3e29f276eac7ecb3887561083acdd1027fb088b9c2b66417cf3488469035`。
- 实机加载：`BPModLoaderMod` 在 Splash、Login、Title 和 MainWorld_5 中生成 `ModActor_C`；只读 Mod 0 同时输出 `READY`。
- 实机瞭望塔：`MainWorld_5` 已确认 22 个唯一 `FastTravelPointID`；`WatchTower_14`、`WatchTower_16` 未出现，保持待观察。详见 `docs/validation/LIVE_TOWER_FAST_TRAVEL_BINDINGS_20260721.md`。

## 阻塞项

1. `WatchTower_14`、`WatchTower_16` 在对应已加载区域的真实 `FastTravelPointID`。
2. 24 个瞭望塔到 22 张遮罩、地图 Location ID 与世界范围的精确映射。
3. 运行时 `SoftUnlockMapMaskTexture` 的真实资产路径，当前仅能取得指针包装显示。
4. 把已经导入 PMK 的原始／势力版图纹理安全接入原生 UMG；不得回退到已崩溃的 Lua UMG 注入。
5. 单机和多人关系状态的权威存储位置与生命周期实证。

## 条件补齐后的实施顺序

1. 启动游戏，确认 UE4SS 与 Mod 0 `READY`，采集地图和传送只读日志。
2. 完成 24 塔、22 遮罩、Location ID、边界的实证表。
3. 生成 `.usmap` / SDK，仅导出本 Mod 涉及的类和表。
4. 先做 `WatchTower_1` 单区域 UI 垂直切片。
5. 接入关系更新与本体文本显示。
6. 同时接入地图和场景公共传送判定。
7. 复制存档完成保存、退出、重载、卸载恢复测试。
8. 单区域通过后再扩展其余已确认区域。
9. 最后重新评估犯罪提示 UI；若仍有状态耦合，继续不做。

当前已经有可安装的 UE4SS Lua Mod 0 开发包，但它是只读探针，不是完整势力版图成品。地图 Overlay、敌对传送限制和存档持久化要继续保持关闭，直到实机映射验证通过。
