# 周六以来完成工作汇总

统计区间：2026-08-22（周六）00:00 至 2026-08-24（周一）16:12（北京时间）
项目：幻兽帕鲁全面战争（PalFactionTerritory）
游戏环境：Palworld v1.0.3.101283 / Steam Build 24575825
报告口径：只统计本区间内新增、修复、部署或取得新验收证据的工作，不重复计算此前已经验收的商人商会落位、基础好感、玩家护卫、NPC 对话等旧成果。

## 一、总体结论

从周六开始，本项目完成了三类集中收敛：

1. 把侧车持久化、世界 80 级、帕鲁部落狂暴、任务与防守、雷恩商人、动态经济和经济战争从“已有代码”推进到 Build 24575825 的实机证据；
2. 补齐好感下降权威后果路由、A8 领主护卫原生生产 Provider、A9 高难奖励原生物品交付等机制缺口；
3. 完成五只已确认唯一帕鲁的 B7 全链路，包括开放、Boss、捕获/击败/超时、NPC 持有、毁灭战争、玩家保卫、势力毁灭、商会赎回、原生交付、重启幂等和崩溃修复。

截至本报告生成时：

- 当前公共源码验证通过：`109` 个 Lua 源文件、`85` 个 Lua 测试、`436` 个受跟踪文本文件；
- 当前 Build 24575825 安装目录通过部署审计：部署清单、当前源码和 `107` 个记录文件哈希一致；
- 周六、周日共形成 `23` 个本地 Git 提交，当前提交头为 `50902f5`；
- 周一新增的 A8、A9、最终 B7 与本报告仍在当前工作区，尚未形成新提交，也没有证据表明已经推送到远端；
- 集中实机后的 SaveGames、Mod State 和正式配置均已按预检快照恢复；游戏当前未启动；
- 正式安装中的 A9/B7 QA 热键和命令文件测试入口均为关闭状态；B7 正式生产调度已启用。

## 二、按日期完成的内容

### 2026-08-22（周六）

#### 1. 好感下降权威后果路由

- 新增统一权威入口 `_G.PWFT_FACTION_CONSEQUENCE_API_V1`；
- 接通成员误伤、平民伤害、任务失败、违约和战争后果五类原因；
- 所有负向事件统一进入既有好感上下界、单次上限、幂等、身份降级、护卫回收、任务权限重算、UI/NPC/商人刷新和侧车持久化链；
- 帕鲁势力继续拒绝负向好感；模型、剧情文本和外部输入不能直接提交结算；
- 完成事件账本、世界 generation 隔离、恢复重绑和冲突拒绝测试。

同时完成 Build 24575825 的原生伤害探针与精确 Actor 适配器。由于当前 Build 的 `OnDamage` 签名和 Attacker 归属还没有取得实机确认，正式伤害扣分仍然失败关闭，不能写成“攻击 NPC 已在游戏内自动扣好感”。

#### 2. 唯一帕鲁 P1/P2 生产基础

- 核验五只已确认唯一帕鲁在当前 Build 中均存在原生 Boss 参数行、Boss Actor 蓝图和生成器候选，不需要挪用其他 Boss 槽位；
- 完成商人商会赎金的原生 ItemShop 购买桥，严格校验商品、势力、价格、数量、库存 1、世界代际和服务器购买结果；
- 完成原生帕鲁交付事务桥：容量预检、单次创建、稳定 `PalInstanceID`、原生捕获、同一实例仓库回读、失败回滚和重复交易隔离；
- 在 Build 24575825 实机完成单只阿努比斯的创建、捕获和同一实例 ID 仓库回读；
- 完成受信内容包可调用的 P2 生产注册器和“付款成功→零商业好感→归属转移→实体交付”的自动组合验收。

周六阶段仍未内置最终赎金价格和正式地图目标；这些随后在周一 B7 五唯一帕鲁内容模块中补齐并完成实机。

#### 3. B0 势力进度侧车持久化

- 同一世界/玩家 profile 连续两次完整退出并重启游戏，均从主侧车恢复 revision 2；
- 故意破坏主 JSON 后，系统从 `.bak` 恢复并重建有效主文件；
- 两个 world GUID 使用独立文件和 profile key，没有串档；
- 除世界期 generation fence 外，耐久玩法状态差异为 0；
- 测试后指定世界存档 `56/56`、Mod State `12/12` 文件恢复，哈希差异为 0。

B0 已关闭，不再需要重复开发同类持久化状态机。

#### 4. B1 世界 80 级

- 最终轮 `35/35` 个合格目标四层等级读回均为 80；
- 覆盖 22 个普通野生帕鲁、1 个阿努比斯 Boss、9 个友军 NPC、3 个商人；
- 玩家保持 29 级，基地/工作帕鲁和玩家公会帕鲁保持原等级；
- Boss 探针按 1 级请求生成，最终四层均为 80，并在 15 秒后回收；
- 快速传送后的新流入目标继续生效；
- 删除了会导致 UE4SS 访问冲突的不安全双回调路线，保留单回调安全实现；
- 测试后 SaveGames `383/383`、Mod State `12/12` 恢复，哈希差异为 0。

正式等级开关在集中验收后恢复关闭。

### 2026-08-23（周日）

#### 5. B2 帕鲁部落狂暴

- 修复 Build 24575825 中普通野生帕鲁 `GroupType=0` 被错误排除的问题；
- 对东南群岛 11 个自然生成实例完成原生字段读回：Predator 开启、HP/伤害倍率 2 倍、生成类型 8、不可捕获、最大 HP 精确翻倍；
- 玩家、5 个拥有/工作帕鲁及其他非世界组在写入前排除；
- 独立 mask miss 负对照中 8 个自然野生帕鲁应用数为 0；
- 全程只消费原生初始化事件，没有 `FindAllOf` 或加载对象广扫；
- 测试后 SaveGames `385/385`、Mod State `12/12` 恢复，正式狂暴与审计开关关闭。

本轮通过的是原生字段和作用域门，没有把未直接拍摄的战斗演出或实际投球失败写成已验收。

#### 6. 商会 Companion JSON 边界修复

- 定位并修复游戏内运行时对象直接越界进入外接 JSON 的问题；
- 商人登记事件改为只投影 6 个文档化字段；
- 公共侧车投影使用原始 `next/rawget`，隔离函数、userdata、thread、循环引用和非有限数字；
- 实机确认 7 个商人商会势力商人和 1 个盗猎集团特殊商人全部登记；
- 外接侧车连续写入 25 个 JSONL 事件，永炎同心会覆盖成功，companion failure marker 为 0；
- SaveGames `384/384`、Mod State `12/12` 恢复。

#### 7. B5 无剧情任务与防守闭环

- 新增 `task_defense_closure.lua`，把权威防守结果、任务目标事件和人类势力好感绑定到同一事件 ID；
- 实机从 FTPoint24 到达小型聚落，生成 4 名原生攻击者并确认 `4/4` 战斗就绪；
- 玩家所属帕鲁完成击杀，头领击杀归因有效；
- 权威胜利结果只为雷恩盗猎集团结算一次防守好感 `+50`，示例任务推进一次并完成；
- 同事件重放、玩家缺席、失败和缺少映射等负向分支由自动测试确认不会重复加好感；
- 验收内容模块默认关闭、不含剧情正文、不写 Palworld 原生存档。

#### 8. B4 雷恩全图鉴帕鲁商人关系往返

- 友好时进入 Palworld 原生帕鲁购买界面，目录包含 288 个唯一图鉴条目；
- 敌对时关闭交互、禁止商店访问、启用 AI 和战斗模式并攻击玩家；
- 再次转为友好后，由原生生成器回收敌对实例并生成单个友好实例，恢复同一商店目录；
- 修复附近旧 Dark Trader 被回退扫描误认后形成孤儿/重复商人的问题；
- 最终干净重跑全部使用 `spawner-handle`，没有使用附近扫描结果；
- 截图、日志、结构化验证和环境恢复均已归档。

#### 9. B6 动态经济与经济战争

- 七个人类势力、八类矿产账本成为动态经济权威输入；
- 七个既有柜台按资源量实时刷新原生商店行，不重新生成商人；
- 雷恩金属矿链实机完成：`150→50→49→150`，对应商店结果为 `240×66→280×25→采购290×62→240×66`；
- 经济战争状态完成 `trade_requested→threat→war→完整重启→war→ceasefire→stable`；
- 修复 UE4SS `TArray` 不能使用 `pairs()` 遍历的问题，并增加当前 Build 形状的自动测试；
- 误入另一世界后立即退出并精确恢复，不把错误世界当作验收样本；
- 验收后 Steam 云续传恢复为正常对勾，主 `Level.sav` 与 `LevelMeta.sav` 哈希保持一致。

#### 10. B7 战略世界统一就绪门闩

- 新增 `strategic_world_readiness.lua` 和机器合同；
- 把内容模块、唯一帕鲁/城市/结局定义、战略 Actor、Boss Provider、世界效果 Provider、交付、NPC 态度和结局 Provider 收敛为 9 项门闩；
- 区分 `content-key-required`、`native-bindings-required` 和 `ready-for-b7-live-acceptance`；
- 未满足完整绑定时保持失败关闭，避免再次把“某个 Provider 已注册”误写成“整条战略世界已接通”。

这一天结束时 B7 只有统一门闩和纯开发结论；真正的五唯一帕鲁实机闭环在周一完成。

### 2026-08-24（周一）

#### 11. A8 NPC 领主/重要角色护卫原生生产接线

- 新增 `npc_leader_guard_native_production.lua` 和对应合同；
- 复用已经验证过的原生角色生成、跟随、战斗让权和回收链；
- 支持精确领主 Actor/Class 绑定、确定性落位、编组上限、部分生成原子回滚、全员死亡释放、领主死亡/卸载/换世界回收和毁灭势力抑制；
- 自动生成七个人类势力默认护卫原型，内容作者只需提供真实领主与编组；
- 自动测试、运行时接线和部署通过。

当前没有擅自指定剧情领主，因此真实领主 Actor、两人以上编队、战斗和回收仍待画面级实机；本项准确状态为“生产源码完成并部署”，不是“实机场景已验收”。

#### 12. A9 高难奖励原生物品交付

- 新增持久奖励交付总线和 Build 24575825 原生物品适配器；
- 权威结果先计算难度倍率、保底和频道上限，再以 `operationId + channelId` 写入 Mod 侧车；
- 通过精确 `OwnerPlayerUId` 定位库存，主机使用 `AddItem_ServerInternal`，发放后用 `CountItemNum` 精确读回；
- 实机第一次发放：六棱晶锭 `0→1`，dispatch=1；
- 重放同一 operation 后数量不增长，dispatch 仍为 1，幂等通过；
- 写前持久化栅栏、歧义后禁止自动重发、世界代际隔离和人工核对状态均已实现；
- 测试后 SaveGames `446` 个文件、Mod State `12` 个文件及正式配置精确恢复。

当前已通过单机/监听主机正向链；远端客户端、背包满和 dispatch 后强制中断仍是独立分项，不计入本次 PASS。

#### 13. B7 五唯一帕鲁完整机制

本次完成并进入生产白名单的五组映射为：

| 唯一帕鲁 | 对应人类势力 |
| --- | --- |
| 捣蛋猫 | 雷恩盗猎集团 |
| 阿努比斯 | 帕鲁防卫队 |
| 疾漩鼬 | 帕鲁保护团体 |
| 魔渊龙 | 永炎同心会 |
| 浪刃武士 | 樱岛/月花众路线 |

完成的机制链包括：

- 五只唯一帕鲁使用当前 Build 已核验的原生 Boss 绑定；
- 自动随机开放、开放前通知、关闭和冷却；
- 非唯一帕鲁 Boss 的运行时生成抑制；
- 石板 Boss 级可配置强度、80 级、可捕获和单次归属；
- 捕获、击败、超时三种权威结算；
- 玩家未捕获时随机分配给其他人类势力；
- NPC 持有者对其他势力发起后台毁灭战争；
- 玩家加入目标势力时改走真实原生防卫波次；
- 势力毁灭后停止刷新、城市空置、商会过滤和持久化；
- 商人商会一亿元赎金、库存 1、服务器原生购买、零商业好感；
- 付款后转移归属、原生创建/捕获、同一 `PalInstanceID` 仓库回读；
- 付款完成但交付中断时自动恢复；
- 重启后重复交易拒绝，归属、赎金次数和唯一个体 ID 保持不变。

集中实机结果：

- 五唯一帕鲁矩阵通过：捣蛋猫、魔渊龙完成捕获；疾漩鼬完成击败；阿努比斯、浪刃武士完成超时；
- 后台毁灭战争通过；
- 玩家防卫路线生成 4 名真实攻击 Actor，权威死亡/结果桥通过；
- 一亿元原生报价通过；
- 实际付款后玩家余额由一亿元降至 3，商品售罄、好感增加 0、阿努比斯归属转玩家；
- 原生交付得到并回读固定实例 ID `pal-00000000000000000000000000000001-39D83E9343CD04844E90D4BA1EE467C6`；
- 重启后两次交付恢复轮询均为 attempted=0、pending=0，重复赎金被拒绝，赎金次数保持 1。

天坠之地/空涡龙及另一组新地图组合仍是暂定项，未进入当前 Build 生产白名单，也不计入本次完成结论。

#### 14. UE4SS 交付后崩溃定位与稳定修复

实际付款和实体交付已经成功后，UE4SS 3.0.1 在释放单次延迟 Lua 回调时发生访问冲突。处理结果：

- 保留完整崩溃证据，不把交付成功掩盖为稳定通过；
- 将交付恢复改为持久 `LoopAsync` 轮询；
- 旧 generation 回调不注销，只通过 generation fence 变成 no-op；
- 把世界加载探针合并到同一个长期游戏线程回调，删除第二个一次性延迟回调；
- 最终重启日志中 `Ref was not function`、Hook exception 和 access violation 均为 0；
- 重启幂等、交付状态和固定实例 ID 均保持正确。

## 三、证据分层总表

| 工作项 | 源码/合同 | 自动测试 | Build 24575825 实机 | 正式部署 | 当前结论 |
| --- | --- | --- | --- | --- | --- |
| 好感下降后果路由 | PASS | PASS | 原生伤害扣分未验 | 已随当前源码部署 | 机制完成，伤害实机失败关闭 |
| 唯一帕鲁 P2 交易与交付事务 | PASS | PASS | PASS | PASS | 完成 |
| B0 侧车重启/备份/世界隔离 | PASS | PASS | PASS | PASS | 完成 |
| B1 世界 80 级 | PASS | PASS | PASS | 正式门关闭 | 机制及实机完成 |
| B2 帕鲁部落狂暴字段 | PASS | PASS | PASS | 正式门关闭 | 字段与作用域完成 |
| Companion JSON 边界 | PASS | PASS | PASS | PASS | 完成 |
| B5 任务与防守 | PASS | PASS | PASS | 验收内容默认关闭 | 通用闭环完成 |
| B4 雷恩商人关系往返 | PASS | PASS | PASS，含画面 | PASS | 完成 |
| B6 动态经济与经济战争 | PASS | PASS | PASS | PASS | 完成 |
| B7 就绪门闩 | PASS | PASS | 已用于最终 B7 | PASS | 完成 |
| A8 领主护卫生产 Provider | PASS | PASS | 未做真实领主场景 | PASS | 纯开发完成 |
| A9 高难奖励物品交付 | PASS | PASS | 主机正向与幂等 PASS | 正式 QA 门关闭 | 核心主机链完成 |
| B7 五唯一帕鲁战略世界 | PASS | PASS | PASS | PASS | 五只基础岛唯一帕鲁完成 |

## 四、当前仍未完成或不应误报的内容

1. 好感下降的 Build 24575825 原生伤害正式结算尚未实机打开；任务失败、违约和战争后果的权威路由已经具备。
2. A8 领主护卫缺少内容作者确认的真实领主 Actor 和编组，因此没有画面级实机结论。
3. A9 尚未验证远端联机客户端、背包容量不足和 dispatch 后强制中断。
4. B2 尚未直接录制 Predator 战斗演出和实际投球失败；本次结论只覆盖精确原生字段。
5. 两个新地图唯一帕鲁组合仍为暂定，不在当前生产白名单。
6. 剧情、任务正文、人物台词、最终奖励表、领主姓名和详细战争规则仍属于玩家/内容作者提供的“密钥”，不属于机制基座擅自创作范围。
7. 周一完成项尚未提交或推送；本报告不能替代后续 Git 提交、版本打包和 GitHub 发布记录。
8. 上述实机 PASS 不等于玩家主观验收；所有集中测试都保留了独立证据和恢复边界。

## 五、关键证据索引

- `docs/60_好感下降权威后果事件路由纯开发验收_2026-08-22.md`
- `docs/61_Build24575825原生伤害探针与失败关闭接线_2026-08-22.md`
- `docs/64_唯一帕鲁原生交付事务桥纯开发验收_2026-08-22.md`
- `docs/65_唯一帕鲁P2生产注册与赎金交付组合验收_2026-08-22.md`
- `docs/66_侧车持久化重启恢复与世界隔离实机验收_2026-08-22.md`
- `docs/67_世界80级分阶段实机验收_2026-08-22.md`
- `docs/68_帕鲁部落狂暴B2分阶段实机验收_2026-08-23.md`
- `docs/69_商会Companion_JSON边界修复与实机复核_2026-08-23.md`
- `docs/70_B5任务与防守闭环开发与实机验收_2026-08-23.md`
- `docs/71_B4雷恩商人关系往返画面级实机验收_2026-08-23.md`
- `docs/72_B6动态经济与经济战争实机验收_2026-08-23.md`
- `docs/73_B7战略世界统一就绪门闩纯开发验收_2026-08-23.md`
- `docs/74_A8领主护卫原生生产接线纯开发验收_2026-08-24.md`
- `docs/75_A9高难奖励原生物品结算纯开发验收_2026-08-24.md`
- `docs/B7-唯一性帕鲁机制开发与实机验证报告_2026-08-24.md`
- `evidence/live-tests/build24575825-20260822-progression-sidecar/`
- `evidence/live-tests/build24575825-20260822-world-level-80/`
- `evidence/live-tests/build24575825-20260823-pal-faction-rage/`
- `evidence/live-tests/build24575825-20260823-companion-commerce/`
- `evidence/live-tests/build24575825-20260823-b5-task-defense/`
- `evidence/live-tests/build24575825-20260823-b4-rayne-merchant-visual/`
- `evidence/live-tests/build24575825-20260823-b6-dynamic-economy-war/`
- `evidence/live-tests/build24575825-20260824-a9-native-reward/`
- `outputs/b7-unique-pal-live-test/20260824-140101434/evidence/anubis-ransom-delivery-loopasync-restart-idempotency-pass/`

## 六、最终复核结果

本报告生成前重新执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify-mod0.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\check-mod0-deployment.ps1
```

结果：

```text
PASS public source verification: 109 Lua files, 85 Lua tests, 436 tracked text files scanned
PASS Build 24575825 deployment matches manifest, current source, and 107 recorded hashes
```

因此，截至 2026-08-24 16:12，可以确认周六以来的上述开发成果已经保存在当前工作区，已完成项的源码、自动测试、部署和实机证据边界清晰；仍待的内容集中在少数分项实机、内容作者密钥和 Git 发布，而不是重复开发已经关闭的 B0、B1、B4、B5、B6 或五只基础岛唯一帕鲁链路。
