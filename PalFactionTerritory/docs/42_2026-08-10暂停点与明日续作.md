# 《幻兽帕鲁全面战争》2026-08-10 暂停点与明日续作

更新时间：2026-08-10
目标：记录今天已经完成和已验证的工作，后续不得重复开发或重复验收，除非相关源码再次发生变化。

## 一、已经完成，不要重复做

### 1. 帕鲁 / NPC 论道对话

- 已接通原生交互入口、确认/拒绝、数字选项和退出语义。
- F2 拒绝不会消耗信物；F1 确认后进入对话；选择有效选项后只消耗一次信物并结算一次好感度。
- 本地化文本和选项已经在游戏内原生面板可见。
- 外部 Agent 仍只能提供显示文本和白名单选择建议，不能直接修改好感度、任务、物品或世界状态。
- 结论：核心对话机制和玩家可见交互已经实机通过，不再回到“对话是否能工作”的开发阶段。

### 2. 商人商会七柜台

- 同轮生成 7 个商人实体、7 个势力账户和 7 个唯一 `PFT_Economy_*` 商品行。
- 原生 ItemShop 可以打开、关闭并再次打开。
- 七柜台统一停用时已完成 7/7 实体和 Spawner 回收，运行计数回到 0。
- 已完成真实需求品 `IronIngot` 出售的服务器请求与库存复制确认；确认后结算商业好感度，实机结果为 `applied=2`。
- 幂等确认 ID、失败保留和重试逻辑已有离线测试保护。
- 结论：商会机制、原生商店和需求品出售好感闭环已经完成；不要重新研究商人生成路线。

### 3. 小型聚落帕鲁攻城

- 倒计时结束后通过 `PalNPCManager.SpawnNPCForServer` 原生请求 4 只临时袭击帕鲁。
- 4/4 句柄解析为实体，统一配置为 80 级、Predator、不可捕获、主动 AI、唤醒和战斗模式。
- 居民仇恨 `175000` 高于玩家仇恨 `125000`，并为就近居民设置主目标。
- 原生生成失败时 fail-closed，不再借用聚落原有睡眠帕鲁。
- QA 重置时已确认 `candidates=4 destroyed=4 failed=0`。
- 用户已确认这项上一轮已经完成。以后不得仅因截图没有清楚拍到伤害数字就重新开发攻城路线。

### 4. 离线验证和安全收尾

- `verify-mod0.ps1` 已通过：46 个 Lua 文件、40 项 Lua 测试、260 个受跟踪文本文件。
- 正式运行配置已恢复为 `settlementRaid.executionMode = "native-negotiator"`、`qaHotkeyEnabled = false`、`attendanceSimulation.enabled = false`。
- 安装端 44 个 Mod 0 运行文件与当前源码和部署清单哈希一致。
- QA Harness 已恢复为测试前禁用形态，没有活动的 `enabled.txt`。
- 安装审计通过：Steam Build `24467282`、3 个 Mod PAK、0 个重复挂载、无玄绒龙内容、QA 禁用。
- 测试存档已隔离，受保护快照已还原：377/377 文件，缺失 0、新增 0、变化 0；两个关键世界文件哈希均与快照一致。
- Palworld 和 Steam 已关闭。

## 二、今天没有完成，明天从这里继续

按顺序继续，不回头重做上面的模块：

1. F5 势力状态面板实机验收：打开、刷新、关闭、12 势力数据、分辨率和输入焦点。
2. FTPoint90 商人岛正式落位：确定 7 个柜台的地面坐标和朝向，确保不重叠、不悬空、不挡传送点或密域入口，再写回契约。
3. 人类势力加入代表：原生交互、资格提示、二次确认、重复请求幂等和敌对关系修复后的加入流程。
4. 玩家人类护卫：领队资格、生成、跟随、战斗、倒地、离开和回收。
5. 帕鲁袭击胜负回调与有限信物：只在玩家参与、胜利且取得头领击杀归属时发放一次随机城邦信物；补测缺席和重复事件分支。
6. 内容作者工具链与示例内容包：一键校验 manifest、任务、论道树、本地化键和冲突，但不写官方剧情正文。
7. 更新 README、完成清单和发布说明，完成隐私/禁限资产审计后再提交并发布 GitHub `v1.0.2` source-only。

## 三、明日启动前规则

- 先读本文件和 `41_未完成项收敛计划_2026-08-10.md`，不要凭旧报告重复已经实机通过的商会、论道或攻城工作。
- 只有下一项确实需要实机时才重新制作测试快照并启动游戏。
- “源码完成、离线通过、实机通过、已推送 GitHub”继续使用不同状态，不互相冒充。
- 本地日志和截图作为验收证据保留，但不得进入公开源码发布包。
