# 目录、资料与工具链盘点

盘点时间：2026-07-21（Asia/Shanghai）。盘点基线为创建 `PalFactionTerritory/` 之前的工作区状态。

## 1. 原工作区

- 根目录：`<workspace>`
- 原文件：357 个，共 110,919,079 字节。
- 原目录：`1.0文本资料库/`、`outputs/` 和空提交历史的 `.git/`。
- Git：分支 `master`，没有提交、没有已跟踪文件；原 357 个文件均为用户未跟踪资产。
- 完整路径、大小、时间和 SHA-256：`../evidence/original_workspace_manifest.csv`。

### `1.0文本资料库/`

311 个文件，共 48,936,840 字节。它是文本研究资料库，不是 Mod 工程。

主要内容：

- 版本范围：Steam `v1.0.1 / build 24181527`，简中为主、英文对照。
- 从本机 `Pal-Windows.pak` 定向复制的 108 个中英文本资产。
- 14,716 个唯一文本键，其中 14,712 个具有中英配对。
- 91 条人工主记录、325 条帕鲁目录记录、66 篇日记、869 条 NPC/任务文本、301 个对话组。
- 9 个 Python 脚本，只负责 PAK 文本抽取、目录构建、证据文档生成和一致性校验；没有 Mod 加载、蓝图、C++ Hook 或打包逻辑。
- 内置只读工具 `repak v0.2.3` 及 Oodle DLL，可读写 Unreal `.pak`，当前只用于信息查看和定向抽取。

实际复跑结果：

```text
verify_database.py
records_verified=91, offset_roundtrips=91, raw_json_rows=14716,
hashed_assets=108, errors=0

verify_entity_catalogs.py
pal_records=325, diaries=66, dialogues=869, groups=301,
bilingual_offset_roundtrips=3744, errors=0
```

势力资料的可复用结论：

- 游戏原文直接确认雷恩盗猎团以佐伊所在高塔为据点，并掌控周边区域。
- 已有七个人类势力的本体名称证据：雷恩盗猎团、帕鲁保护团体、永炎同心会、帕洛斯群岛自卫队、基因研究部队、月花众、天坠军。
- 既有关系判定区分“原文明说／文本推断／用户裁定”，可作为叙事参考，但不能冒充运行时 Team/Group 参数。
- 既有文档明确没有 Mod 实现。

### `outputs/`

46 个文件，共 61,982,239 字节，包括 5 个 MP4、5 个 PPTX、5 个 SRT、5 个 PPTX 检查文件、旁白／证据／发布文案以及 11 张演示页 PNG。

这些是宣传与关系图成品，不是游戏资源或可编译工程。检查了配套 Markdown、文本、PPTX 检查记录和 DOCX 正文；它们用于理解产品意图，不作为运行时 API 证据。

## 2. 本机游戏安装

- Steam AppID：`1623730`
- 安装：`E:\SteamLibrary\steamapps\common\Palworld`
- App manifest：build `24181527`，语言 `schinese`，磁盘占用 41,176,665,922 字节。
- 主 PAK：`Pal\Content\Paks\Pal-Windows.pak`，40,526,106,335 字节。
- 当前 `Paks/` 仅有本体 `Pal-Windows.pak`。
- `Mods\PalModSettings.ini` 存在，但 `bGlobalEnableMod=False`。
- 未发现 UE4SS、PalSchema、LogicMods、NativeMods 或已安装 Workshop Mod。

本次没有修改任何游戏文件，也没有开启 Mod 全局开关。

## 3. 本机开发工具

已具备：

- Python 3.10
- Git 2.53
- .NET 8 运行时（未安装 .NET SDK，不能使用 `dotnet build/run`）
- Node.js 24
- Rust/Cargo
- Visual Studio Community 2026，含 MSBuild 和多套 MSVC x64/x86 工具链
- 项目内 `repak v0.2.3`

未发现：

- Unreal Editor / UnrealPak
- Palworld Modding Kit 或任何 `.uproject` / `.uplugin` 工作区
- UE4SS
- PalSchema
- FModel
- UAssetGUI
- 当前游戏 build 对应 `.usmap` 或生成 SDK
- .NET SDK
- PATH 中的 CMake / Ninja

对工作区、Codex 工作目录和 Steam 公共安装目录做了文件名级递归搜索，没有找到上述 Mod 工具或工程。

## 4. 关键资源证据

使用项目内 `repak` 从本体 PAK 定向复制了 173 个关键资产，共 9,320,163 字节，保存于 `../evidence/game_assets_raw/`。副本的路径、大小和 SHA-256 见 `../evidence/extracted_asset_manifest.csv`。

抽取范围：

- 世界地图数据与 `WBP_Map_*` UI
- 原始地图遮罩材质与 Render Target
- 20 张本体解锁区域遮罩
- 瞭望塔解锁点与快速传送对象
- 解锁地图 Cutscene 蓝图
- 犯罪提示 UI 与犯罪数据表

这些副本仅用于分析，不会作为 Mod 重分发内容。

## 5. 已确认缺口

当前二进制资产是未版本化属性格式。没有当前 `.usmap`／SDK 时，可以确认类、函数、变量和依赖名，但不能可靠还原所有属性值、蓝图图线和 24 座瞭望塔到 20 张遮罩的精确绑定。此缺口直接阻止安全制作可安装版本。
