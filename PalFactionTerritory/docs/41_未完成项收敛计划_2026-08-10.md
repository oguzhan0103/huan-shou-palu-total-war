# 《幻兽帕鲁全面战争》未完成项收敛计划

更新日期：2026-08-10

目标版本：开源机制基座 `v1.0.2`

## 1. 发布边界

本轮继续交付“基座”，不在 Core 中写死剧情、任务正文、角色台词、信物名称或详细世界设定。机制、状态机、公共 API、内容包接口、失败回滚、离线测试和实机验收工具属于基座；剧情与详细设定由内容作者通过内容包提供。

状态口径固定为四类：

1. **已实机通过**：当前目标 Build 中已有同轮日志与画面证据。
2. **离线完成**：源码、契约和自动测试通过，但尚未在当前 Build 中完成玩家可见验收。
3. **待实机验收**：技术路线和测试入口已经存在，只差集中进游戏确认。
4. **未实现**：仍缺源码、Cooked 资产或必要的内容输入。

离线测试通过不得写成实机通过。发布包继续排除 Pocketpair 游戏资产、Cooked PAK、存档、日志、Steam 身份、绝对本机路径、API Key 和历史玄绒龙资源。

## 2. 当前完成基线

### 已实机通过

- 小型聚落帕鲁攻城：倒计时后原生生成 4 只袭击者、居民仇恨优先、失败关闭、活动结束 4/4 回收。
- 商人商会七柜台：7 个实体、7 个势力账户、7 个商品行、原生 ItemShop 可连续打开、7/7 回收。

### 离线完成

- 7 人类势力、5 帕鲁势力、12 岛屿区域、关系矩阵、多势力加入、四级身份、商业额度、护卫资格和结局门槛。
- 商会供需、加工品成本、价格、库存、需求品采购、重复事件保护和商业窗口封顶。
- 帕鲁袭击事件规范化、头领击杀归属、随机城邦信物、固定配额、文本任务门闩、有限论道、技术故障返还和红转蓝结算。
- 内容包注册、版本/依赖/冲突校验、任务、战略世界、结局和本地化键接口。
- 外部 Agent 运行时、严格文件桥和 Core 对话控制器。模型只能生成显示文本和白名单选择建议；玩家确认后才由确定性规则引擎执行选择。
- 商会出售复制成功后的好感度结算具备稳定确认 ID、窗口 ID、失败保留和幂等重试；正式结算开关仍等待实机信号验收。

## 3. 收敛顺序

| 优先级 | 工作包 | 当前状态 | 完成标准 | 是否需要实机 |
| --- | --- | --- | --- | --- |
| P0 | NPC / 帕鲁对话控制器接入外部 Agent | 离线完成 | 请求、轮询、字段白名单、离线回退、玩家确认、确定性结算全部回归通过 | 否 |
| P0 | 原生代表交互与对话呈现 | 开发中 | 靠近已注册代表才可开启；显示本地化节点和选项；自由文本可送入 Agent；关闭、失败、重复输入均不重复结算 | 是 |
| P0 | 商会真实出售好感闭环 | 离线候选完成 | 需求品真实出售后仅结算一次；取消、失败、非需求品均为 0；临时结算失败可用同一确认 ID 重试 | 是 |
| P0 | GitHub `v1.0.2` 诚实发布 | 待本轮源码冻结 | README、完成清单、计划、测试结果与源码一致；不含禁限资产；Actions 通过 | 否 |
| P1 | F5 势力面板 | 待实机验收 | 当前 Build 中可打开/关闭、12 势力数据正确、分辨率和输入焦点正常 | 是 |
| P1 | FTPoint90 商会正式落位 | 待实机验收 | 七柜台不重叠、不悬空、不挡传送点或密域入口；坐标与朝向写回契约 | 是 |
| P1 | 原生帕鲁袭击胜负回调 | 离线适配器完成、原生绑定未开 | 只在玩家参与、玩家方胜利且取得头领击杀归属后发放一次信物 | 是 |
| P1 | 人类势力加入代表 | 公共加入 API 完成、原生交互待接 | 代表 NPC 能显示资格、二次确认并加入；失败与重复请求幂等 | 是 |
| P1 | 玩家人类护卫 | 资格和提供器接口完成、跟随实机待验 | 领队起出现护卫；生成、跟随、战斗、倒地和回收均可验证 | 是 |
| P2 | 内容作者工具链 | 基础 SDK 完成 | 一条命令校验 manifest、任务、论道树、本地化键和冲突；生成可读错误报告 | 否 |
| P2 | 商会数值校准 | 首版完成、长期平衡待反馈 | 七势力矿产存量能解释售价；缺口资源能解释采购需求；版本化调整不破坏旧内容包 | 可后置 |
| 外部输入 | 剧情与详细设定 | 不属于 Core | 任务正文、代表台词、信物名称、城市剧情和结局叙事由内容作者提供 | 否 |
| 已撤出 | 玄绒龙 | 不再开发 | 不进入活动 Mod、公开包或测试清单 | 否 |

## 4. 当前开发批次

### 批次 A：对话与商业确定性闭环

- [x] 新增 `pal_dialogue_controller.lua`。
- [x] 将论道会话补齐代表 ID 与代表名称键。
- [x] 对外部模型响应再次执行 Core 侧字段白名单和选择白名单校验。
- [x] 模型不可直接修改好感度、任务、物品、经济、存档或世界状态。
- [x] Agent 不可用、超时或返回非法字段时保留离线对话树。
- [x] 建立通用呈现路由，统一展示本地化节点、固定选项和 Agent 对白。
- [x] 建立代表精确 Actor 绑定与 500 单位距离门闩。
- [x] 显示后端未就绪时在不可逆确认前阻止开始；显示启动失败走技术故障返还。
- [x] 商会出售复制确认的临时失败保留待结算记录。
- [x] 重试复用同一确认 ID 与商业窗口，成功后删除记录并阻止重复加分。
- [ ] 接入玩家可见的原生代表交互与对话呈现。
- [ ] 在同一实机轮次验证自由文本、离线选项、玩家确认和退出语义。
- [ ] 验证真实需求品出售、取消、失败、非需求品和重复复制事件。

### 批次 B：现有玩家入口集中验收

- [ ] 验收 F5 势力面板。
- [ ] 在 FTPoint90 记录商会正式地面坐标与七槽朝向。
- [ ] 验收人类加入代表和领队护卫。
- [ ] 将原生帕鲁袭击结算回调绑定到有限信物服务。

### 批次 C：内容作者与发布

- [ ] 补充最小论道内容包和代表绑定示例，但不写官方剧情正文。
- [ ] 补充内容包一键校验命令与错误报告。
- [ ] 更新根 README、完成清单和发布说明。
- [ ] 执行 Lua、Rust、PowerShell、隐私和禁限资产审计。
- [ ] 只提交源码、契约、测试、示例和文档，发布 `v1.0.2` source-only 版本。

## 5. 集中实机验收门槛

游戏只能在用户明确发出实机测试指令后启动。集中测试按以下顺序执行，减少反复重启：

1. 只读确认 Build、Mod 挂载、正式配置和测试存档隔离状态。
2. F5 面板：打开、刷新、关闭、不同分辨率和输入焦点。
3. 商会正式落位：七柜台、七商品行、连续交互、购买、出售、额度和回收。
4. 人类代表：查看资格、确认加入、重复请求和敌对修复后加入。
5. 护卫：领队资格、生成、跟随、战斗、倒地、离开和回收。
6. 帕鲁代表：靠近门闩、离线论道、Agent 自由文本、玩家确认、主动退出、技术故障返还。
7. 帕鲁袭击结算：参与、胜利、头领归属、一次性信物和缺席分支。
8. 退出世界后核对临时实体全部回收、Mod 侧车可恢复、Palworld 原生存档未被直接改写。

## 6. 本轮已经开始的结果

本轮不是只列计划：批次 A 的 Core 对话控制器和商会结算重试已经落地并通过完整离线回归。下一步继续开发代表交互/呈现适配层；所有需要当前 Build 画面或真实交易结果的项目保持“待实机验收”，不会提前改成完成。
