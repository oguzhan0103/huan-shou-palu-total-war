# 商人商会真实交易与商业额度验收（2026-08-15）

## 结论

本轮状态：**PASS（商人商会 1.0 验收完成）**。

云存档恢复后，在 Steam Build 24575825 的指定测试存档中完成了雷恩商会柜台的真实购买、需求品出售、非需求品出售、复制回调去重、单窗口 20 点额度封顶，以及敌对状态下跨三个窗口的 60 点商业修复。原生金币、商品库存和玩家背包均发生了实际变化；好感度只在服务端出售请求及背包复制确认后结算。最终游戏内 F5 面板显示雷恩盗猎团由红色敌对恢复为蓝色友好，好感从 2 增至 62，独立雷恩帕鲁商人也按关系变化重生并重新开放交互。用户于 2026-08-16 确认：七柜台共用同一实现路线，2/7 柜台真实成交足以代表七柜台机制成立，商人商会可按已完成处理。

## 本轮已通过

- Build 24575825 下，临时全柜台入口一次生成 7 名普通商人，结果为 `activeAfter=7`、`pendingAfter=0`。
- 7 名商人分别登记到 7 个不同人类势力；雷恩全图鉴帕鲁商人仍是独立特例，不计入商会 7 人。
- 7 个柜台分别绑定到唯一 `PFT_Economy_*` 行，均出现原生交互初始化、势力登记和商店绑定成功日志。
- 走近柜台按 F6 能打开原生 ItemShop。抽查永炎同心会柜台，画面与日志均确认打开 `PFT_Economy_Pyre`，显示金属铸块、精炼金属铸块、碳纤维、火药、聚合物和塑料等该势力商品。
- 雷恩商会柜台真实购买 1 个金属锭：商品库存 `66→65`、金币 `5544→5304`、玩家获得 `金属锭×1`，日志出现 `ECONOMY_BUY_CONFIRMED quantity=1 award=1`。
- 七柜台同轮抽查中，永炎同心会与帕洛斯群岛自卫队柜台分别真实购买 1 个金属锭，两次均为 `award=1`。结合 7/7 生成、唯一商品行、交互和商店绑定，用户确认该 2/7 代表性成交已满足整组柜台验收标准。
- 把该金属锭从原生出售页卖回后，物品消失、金币 `5304→5328`，日志只出现一条 `NATIVE_SELL_REPLICATION_CONFIRMED applied=0 reason=no-requested-items`。这证明非需求品正常成交但不增加好感度。
- 需求品 `IronIngot` 的 40 件实机交易已出现服务端请求、背包复制确认和 `applied=2 reason=award-applied`；同样的第二笔交易也只结算一次。
- 新增 400 件批量测试入口后，同一原生商业窗口完成了 `购买 +1`、三笔需求品出售 `+8`、`+8`、`+3`，累计恰好 20。第四笔 400 件出售仍正常成交并增加金币，但日志为 `applied=0 reason=award-capped`。
- 每笔出售均只有一个服务端请求和一个最终复制确认，没有被 StackCount/ItemId 或临时展示槽重复结算。
- 当前测试角色金币不足时，UI 会在客户端直接阻止购买，没有产生服务端失败结果。因此“服务端失败购买不加好感”仍不能用这次点击验收。
- 商业窗口测试适配器已完成：默认关闭，只能生成 3 个有界 QA 窗口；它只更换 `windowId`，不直接写好感度。正式运行仍使用正常窗口来源。
- QA 物资/出售入口补上了 UE4SS 排队回调强引用。F11 的 40 件和 Ctrl+F3 的 400 件发放、F12 对当前原生商店的出售均已取得实机结果；这些入口只用于验收，正式配置默认关闭。
- 修复了普通商品整组卖出时的确认缺口：商店传入的临时展示槽不再被误当作唯一库存权威，结算器会在任一真实背包 OnRep 后核对该物品的聚合数量变化。数量没有下降时仍然 fail-closed。
- 新增的敌对商业验收适配器通过真实 `join_human` 加入帕鲁保护团体，按已登记的势力关系把雷恩切换为敌对；日志明确记录 `directReputationWrites=false`，没有直接改写好感度。
- 雷恩敌对后，商会柜台仍依照 `commercialTruce` 规则提供交易；独立雷恩帕鲁商人则记录 `SHOP_ACCESS_BLOCKED`，交互关闭，二者职责没有混淆。
- 三个有界窗口均通过真实需求品出售完成 `+8/+8/+4=20`，合计 60；最后一笔确认后日志记录 `relation-change-Friendly`，新商人实例为 `relation=Friendly`、`interactEnabled=true`，并重新绑定 `PFT_Rayne_AllPaldex`。
- 最终 F5 画面显示 `[蓝]雷恩盗猎团 | 好感 62 | 可加入 | 商业额度 0`，从敌对起点好感 2 完整增加 60，证明“敌对只能主要靠商业慢慢修复”的跨窗口链路可执行。

## 自动验证

`scripts/verify-mod0.ps1`：PASS。

- 68 个 Lua 源文件
- 61 个 Lua 测试
- 324 个受跟踪文本文件扫描
- 商业窗口：3 个有界原生交易窗口、无直接好感写入
- 商会运行时：7 柜台、唯一商品行、交互路由、回滚和生命周期测试通过
- 商业结算：购买成功回调、出售服务端请求和背包复制确认的离线契约通过

## 验收矩阵

| 项目 | 状态 | 本轮证据或边界 |
|---|---|---|
| 雷恩柜台真实购买 | PASS | 库存、金币、玩家物品与 `ECONOMY_BUY_CONFIRMED award=1` 同时成立 |
| 需求品出售 | PASS | 40 件为 `applied=2`；400 件按 `8/8/3/0` 结算 |
| 非需求品出售 | PASS | 金属锭真实成交，唯一确认 `applied=0 reason=no-requested-items` |
| 重复复制回调去重 | PASS | 每笔服务端出售请求只有一个最终确认和一次好感结算 |
| 单窗口 20 点封顶 | PASS | `1+8+8+3=20`；之后交易成功但 `applied=0 reason=award-capped` |
| 七柜台代表性真实购买 | PASS | 七柜台生成、唯一行、绑定和开店均为 7/7；永炎与自卫队完成 2/7 同轮真实成交，用户确认足以验收共用路线 |
| 取消出售 | NOT REQUIRED | 未形成同轮审计证据，但用户确认不作为商人商会完成门槛；正式结算仍要求服务端请求和复制确认 |
| 服务端失败购买 | NOT REQUIRED | 客户端会直接拦截，未产生失败回调；用户确认不作为商人商会完成门槛，现有结算继续 fail-closed |
| 敌对商业修复 20×3 | PASS | 三个窗口分别 `8/8/4`，累计 60；关系 `Hostile→Friendly`，F5 为蓝色、好感 `2→62`，独立雷恩商人重新开放交互 |

## 收尾与安全状态

- 游戏已关闭，Palworld 进程为 0。
- 本轮测试前的本地存档、UE4SS 与 3 个 Mod PAK 均已从预检快照恢复；存档树 447/447、UE4SS 树 159/159、资产 PAK 2/2、逻辑 PAK 1/1，逐文件 SHA-256 均为 0 缺失、0 变化、0 多余。
- 已授权测试世界的关键存档文件均恢复为测试前逐文件哈希；存档名称和哈希不进入公开仓库。
- 临时 QA 框架的安装启用标记已删除；安装审计确认全部 QA 框架为 disabled。
- 正式配置已重新部署：商会生产驻场开启，Ctrl+F9 柜台测试与商业窗口测试均恢复为默认关闭。
- 安装审计 PASS：3 个 Mod PAK、0 个重复挂载、无已撤回玄绒龙资产、无新崩溃目录。
- QA 框架在 23:45 有一条实验性 ItemShop 小部件捕获失败日志；该路径未被当作验收证据，之后的原生交易、关系恢复和世界卸载均继续成功，正式安装中 QA 框架已禁用。
- 世界卸载日志为 `WORLD_RECORDS_ABANDONED count=0`、`ECONOMY_MERCHANT_PRESENCE_WORLD_UNLOADING cleanup=true`；测试商人回收为 `active=0`。
- 测试期间的变更副本和完整日志保存在 `outputs/live-test-postrun-quarantine/build24575825-20260816-000810710`，未提交、未推送、未发布。

## 后续边界

商人商会已经完成，不再安排七柜台逐一购买、取消出售或服务端失败购买专项。除非商会源码、原生商品资产或 Palworld Build 发生相关变化并出现反向证据，否则不得重复该验收。取消/失败分支以后若顺手取得证据，只作为覆盖增强，不改变本项完成状态。
