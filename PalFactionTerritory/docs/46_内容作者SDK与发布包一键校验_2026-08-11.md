# 内容作者 SDK 与发布包一键校验

日期：2026-08-11
目标版本：`v1.0.3` 候选版

## 结论

内容作者工具链已经达到 source-only 基座的离线完成标准：最小内容包同时覆盖 manifest、任务、战略世界、结局路线、本地化目录和帕鲁论道；仓库与发布 ZIP 都能用一条 PowerShell 命令完成原子校验。

示例只包含作者占位文本，不包含官方剧情、任务正文、信物名称或详细世界设定，也不携带 Pocketpair 资产。

## 仓库内校验

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate-content-pack.ps1 -PackPath examples/minimal-content-pack
```

## 发布 ZIP 内校验

```powershell
powershell -ExecutionPolicy Bypass -File AuthorSDK/validate-content-pack.ps1 -PackPath AuthorSDK/minimal-content-pack
```

校验器会自动识别两种目录布局，加载对应的 Core 脚本，然后在临时运行时中原子注册整包。任一领域失败时整包不提交。

## 已覆盖的拒绝条件

- 非法或冲突的 manifest、命名空间和 SemVer；
- 缺失依赖、显式冲突和加载顺序不确定；
- 未声明或跨包占用的本地化 key；
- 非法任务阶段、不可达分支或不一致的结构化结果；
- 非法论道树、循环、不可达节点或不终止路径；
- 任务、战略世界、结局、论道和本地化的内容包 ID/版本不一致；
- 模型或文本层试图直接修改确定性状态。

## 发布包验证

`v1.0.2` 的包结构烟测曾通过；`v1.0.3` 候选版已经从最终冻结工作树重新生成并复验：

- `scripts/build-mod0-package.ps1` 先执行当前全量 Mod 0 验证；冻结工作树结果为 `65` 个 Lua 文件、`58` 个 Lua 测试和 `296` 个受追踪文本文件；旧版 49/42/260 以及中间工作树统计不作为新版本证据；
- ZIP 包含 Core、五份内容契约、完整最小内容包以及两份校验器文件；
- 从解压后的 staging 目录再次运行发布版校验命令，返回 `PWFT content-pack validation: PASS`；
- 最终本地候选包名为 `PalFactionTerritory0-v1.0.3-build24575825.zip`，SHA-256 为 `e71db853dfd06c8471cfa227425299c3f6e91b91fffe7aee78607164bdd03a7a`；它不复用任何中间候选值或 `v1.0.2` 历史哈希。

仓库级 source-only 构建器同时生成 Core、Official Add-ons 和 AI Experimental 三包，并执行精确 ZIP 条目、重复条目、逐字节内容、密钥/本机路径、禁限扩展名和确定性重建检查。冻结候选结果如下：

| 包 | payload 文件数 | SHA-256 |
| --- | ---: | --- |
| Core source-only | 167 | `765515f8f990591d05603cc50ebe8ca702ccc0474167cdf9b99b87a4786a2f32` |
| Official Add-ons source-only | 14 | `208d27820c9d273b6add71e115ec7f6405af5f72d3ab241f5258cbad3680e560` |
| AI Experimental source-only | 37 | `abc37eaf2d4eff99c0c3dbc0fa0aaf6737a0843e5ec0f05a3f96fece4e074745` |

AI Experimental 的 Windows 二进制包仅供本地开发/侧车验证，不属于公开 source-only Release；其冻结候选 SHA-256 为 `e1554fae8fde83fb4465b18427bb959219cc6119fde93ce9db60c9c225f40ff9`，并明确要求二进制再分发前单独完成依赖 SBOM 与许可证审阅。

这些候选包位于本地输出区，不等同于 GitHub Release；远端发布仍需完成提交、推送和新提交的 Actions 验证，且不得移动既有 `v1.0.2` tag。
