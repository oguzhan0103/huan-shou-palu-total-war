# Build 24575825 世界 80 级 B1 实机验收

- 结果：`PASS`
- 日期：2026-08-22
- 游戏版本：v1.0.3.101283 / Steam Build 24575825
- 范围：B1 等级；不包含 B2 狂暴、不可捕获和双倍血伤

## 通过摘要

1. 35 个合格目标应用并验证，35/35 的 effective、component、individual 和 override 均为 80。
2. 覆盖 22 个普通野生帕鲁、1 个请求为 1 级的 Anubis Boss、9 个友军 NPC 和 3 个商人。
3. 玩家保持 29 级；5 个基地／工作帕鲁与 14 个玩家公会帕鲁保持原等级。
4. Boss 15 秒后销毁；快速传送到小型聚落后的新流入目标继续通过。
5. 所有等级处理来自精确原生初始化事件；四次审计均为 `broadScan=false`。
6. 最终轮没有新崩溃；完整 SaveGames 383/383 和 Mod State 12/12 已恢复，正式安装 76 项审计通过且高风险门默认关闭。

## 边界

公开仓库只保留机器可读摘要，不包含完整 UE4SS 日志、崩溃包、玩家存档、State 或本机绝对路径。该结果是代理执行的实机验证，不冒充用户主观验收，也不关闭 B2。

公开复核：

```powershell
python tools/verify_world_level_live_evidence.py
```
