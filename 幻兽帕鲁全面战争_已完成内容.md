# 《幻兽帕鲁全面战争》已完成内容

更新日期：2026-08-10

公开版本：v1.0.2 开源机制基座

目标游戏版本：Steam Build `24467282`

## 一句话结论

已经完成并公开的是“无剧情机制基座”和若干独立扩展源码，不是完整剧情成品。小型聚落帕鲁攻城与商人商会七柜台已经在 Build `24467282` 实机通过；势力规则、商业模型、内容包 SDK、有限论道、AI 对话控制器、通用呈现路由和代表交互门闩已完成源码与离线测试，但原生对话 Widget、NPC Delegate 和部分玩家可见入口仍未接通或默认关闭。

## 1. 已完成并实机确认

### 小型聚落帕鲁攻城

- 倒计时后通过 `PalNPCManager.SpawnNPCForServer` 原生生成袭击者。
- 两轮测试均完成 4/4 实体解析、配置、唤醒和战斗激活。
- 居民仇恨 `175000` 高于玩家 `125000`，袭击者优先攻击聚落居民。
- 生成失败时 fail-closed，不会把聚落原有野生帕鲁改造成袭击者。
- 活动结束、QA 重置和世界重载均能显式回收本轮 4/4 临时实体。
- 玩家缺席分支只做后台结算，不生成世界战斗，也不写 Palworld 原生存档。

### 商人商会七柜台

- 同一轮生成 7 个商人实体，分别绑定 7 个人类势力和 7 个唯一 `PFT_Economy_*` 商品行。
- 完成原生商人生成、交互球初始化、势力登记和商店网络绑定。
- 玩家能够打开原生 ItemShop，关闭后再次交互仍能正常打开。
- 测试结束后 7/7 商人和对应 Spawner 均被回收。
- 商品、库存与价格显示已确认；测试角色当时货币为 0，因此真实购买与商业好感度结算没有被标为实机通过。

详细证据见 `PalFactionTerritory/docs/40_帕鲁攻城与商人商会最终实机验收_2026-08-08.md`。

## 2. 已完成源码与离线验证

### 势力与世界规则

- 7 个人类势力、5 个帕鲁势力、22 个岛屿区域和版本化关系合同。
- 玩家可加入多个人类势力；身份为成员、核心成员、领队、领主。
- 加入资格、敌对关系覆盖、商业修复、任务/防守/商业好感度、领队护卫资格和结局三门槛均有确定性 API。
- 任务、内容包、战略世界、唯一帕鲁、城邦和三路线结局均有版本化合同与状态机。

### 商业与商会

- 七岛矿物供给、加工品成本、售价、库存、采购价和采购额度模型。
- 7 个商品组、7 个抽选组、26 条出售商品和 37 条采购需求的数据合同。
- 商户登记、商店绑定、需求品判定、重复事件防护和商业窗口封顶逻辑。
- 真实服务器出售成功信号确认前不结算好感度；正式生成与写入开关默认关闭。
- 出售复制确认后的结算使用稳定确认 ID 和商业窗口 ID；临时结算失败保留待处理记录，重试成功后只入账一次。

### 帕鲁信物与有限论道

- 玩家参与、玩家方胜利且取得袭击头领击杀归属后才允许发放信物。
- 信物在七个城邦中随机，允许重复；每个内容包可定义固定配额。
- 每枚信物对应一次任务与一次论道机会；确认前取消不消耗，技术故障返还，正常使用后消耗。
- 次数耗尽仍未达门槛时永久无法和解；人类势力条件满足后，帕鲁关系只能由红转蓝。
- 离线对话树只保存本地化键和确定性分支，基座不内嵌剧情正文。

### 内容作者 SDK

- 内容包命名空间、版本、依赖、冲突、加载顺序和能力声明。
- 本地化键、任务、代表、论道树、战略世界和结局的最小示例包。
- 跨领域加载失败时的原子回滚与离线端到端测试。

### 外部 AI 对话实验

- Rust 外部运行时支持本地 Ollama 和 OpenAI-compatible 接口。
- UE4SS 文件桥具备请求、轮询、幂等处理和严格字段校验。
- Mod Core 已接入 `pal_dialogue_controller`：按当前论道节点生成白名单请求、轮询响应，并在 Agent 不可用或输出非法时回退离线树。
- 通用呈现路由可向任意后端输出代表名称键、节点文本键、固定选项和 Agent 对白；固定选择、模型建议确认、主动退出和技术故障分别走确定性 API。
- 代表交互路由要求内容包已注册的代表、精确绑定的 Actor 和 500 单位内距离；显示后端未就绪时不开始不可逆会话，显示启动失败时走技术故障返还。
- 模型只能返回对白、白名单选择和白名单标签，无权直接修改任务、好感度、物品、经济或世界状态。
- 本地长期记忆、前台进程门禁、密钥不落盘、错误脱敏和 mock provider 测试已实现。

### 多帕鲁独立扩展

- `PalMultiOtomo0` 使用 F6 最多增加 4 只辅助战斗帕鲁，F7 逐只回收并保留原版主帕鲁。
- 不使用 Tick，不写存档，不修改原始 PAK。
- 源码、安装脚本和离线烟测已公开；当前 Build 的重新实机确认不在本次发布证据内。

## 3. 明确未完成

- NPC / 帕鲁代表 Actor 的原生 `OnTriggerInteract` Delegate、游戏内对话 Widget 后端和玩家可见论道 UI。
- 已完成的外部 AI/Core 呈现路由与原生 NPC 展示层的最终接线及实机验收。
- 商业真实购买、需求品出售复制确认、补差与限额好感度的完整实机闭环。
- F5 势力状态面板的当前 Build 实机输入与分辨率验收。
- 商人商会在 `FTPoint90` 小岛上的最终地面坐标、朝向与场景摆放。
- 正式剧情、任务正文、信物名称、角色设定、代表对白和完整结局内容。
- 玄绒龙；该模块已撤出公开范围，不包含白模、美术资源或历史 PAK。

## 4. 开源交付边界

GitHub 仓库公开第一方源码、合同、测试、作者示例和文档。Release 附件是可校验的 source-only 分包，不含：

- Pocketpair 或其他权利人的游戏资产；
- `.pak`、`.uasset`、`.uexp`、模型、纹理或音频；
- 玩家存档、日志、崩溃转储、Steam 身份信息；
- 本地构建目录、工具二进制、API Key；
- 玄绒龙工程与美术资源。

因此，`PalMultiOtomo0` 可作为独立 UE4SS 脚本扩展安装；`PalFactionTerritory0` 的公开包主要面向开发者和内容作者，不能被描述成包含全部 Cooked 资产的一键安装成品。

## 5. 验证结果

- `PalFactionTerritory`：42 个 Lua 文件、36 项 Lua 测试通过。
- 根仓库：公开源码 Lua 语法与测试总门禁通过。
- `PalAgentDialogue`：Rust 单元/集成测试通过；游戏内展示仍待接线。
- 公开发布扫描不包含游戏二进制、Cooked 资产、存档、日志或密钥。

仓库地址：<https://github.com/oguzhan0103/huan-shou-palu-total-war>
